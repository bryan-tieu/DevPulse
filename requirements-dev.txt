# Dev/CI tooling — not needed at runtime (kept out of requirements.txt).
# Install into the .venv: pip install -r requirements-dev.txt
ruff
black
pytest
httpx2  # fastapi.testclient transport — tests only; this starlette wants httpx2, not httpx
