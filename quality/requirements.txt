# Pinned 2026-07-09 (reproducible builds, no surprise minor bumps between runs).
# pandas held at 2.x deliberately: 3.0 shipped breaking dtype/copy-on-write
# changes weeks before this pin; GE declares pandas>=1.3 with no ceiling, which
# means untested against 3.0 — not today's debugging budget.
great-expectations==1.18.2
pandas==2.3.3
pyarrow==24.0.0
gcsfs==2026.7.0
