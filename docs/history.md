# Build History — DevPulse

> The day-by-day journal of what landed, moved out of `CLAUDE.md` to keep session context lean.
> Each entry links to its full day plan in `docs/daily/` and its decisions in [decisions.md](decisions.md).
> **Append one entry here at the end of each day** (the `/end-session` skill does this).

---

## Day 1 — Repo skeleton & GCP foundation (2026-06-13)

Repo skeleton, Python `.venv`, `.gitignore`, GCP account + billing alerts.

## Day 2 — Terraform cloud foundation (2026-06-14)

Terraform provisions the GCS bronze bucket, BigQuery `devpulse_silver` dataset, and least-privilege pipeline service account (applied & verified). Pipeline SA: `devpulse-pipeline@devpulse-dp2622.iam.gserviceaccount.com`.

## Day 3 — End-to-end thin slice (2026-06-15)

`ingestion/` downloads one GH Archive hour → GCS bronze (Hive-partitioned, idempotent via `blob.exists()`), `transform/` counts events by type → BigQuery `hourly_event_counts` (WRITE_TRUNCATE), `api/` serves `/event-counts` over the table. Run as me via ADC; verified end-to-end then `terraform destroy`ed.

## Day 4 — Airflow in Docker (2026-06-16) · [plan](daily/day-02.md)

Airflow in Docker (LocalExecutor): trimmed the official compose stack (no Celery/redis/worker), extended the image with the GCP libs, mounted code + ADC read-only into the containers, and wrapped the unchanged `ingest_hour`/`load_event_counts` in an `@hourly` `ingest >> transform` DAG that derives `date`/`hour` from the run's data interval (`catchup=False`, `retries=2`). Triggered the 2024-01-01 15:00 hour: both tasks green, bronze object + `hourly_event_counts` matched Day 3, and a clear/re-run proved idempotency (`SUM(event_count)` unchanged).

## Day 5 — Partitioned silver table, sensor, bounded backfill (2026-06-19)

Made `hourly_event_counts` **HOUR-partitioned** on `event_hour` and switched the load to a partition decorator (`table$YYYYMMDDHH`, `WRITE_TRUNCATE`) so each hour replaces only its own partition (lifting the whole-table truncate that pinned Day 4 to one hour); table created idempotently (`create_table(exists_ok=True)`). Added a `wait_for_archive` PythonSensor (`mode="reschedule"`, HEAD on the GH Archive URL) gating `ingest`, plus `max_active_runs=1` so the in-memory transform can't OOM under backfill fan-out. Ran a bounded `dags backfill -s 2024-01-01 -e 2024-01-03` (48 contiguous hours, 0 failures), verified partition-scoped idempotency (re-loading an hour left the grand total + that hour's sum unchanged), and cleaned up stray live-scheduler runs.

## Day 6 — The PySpark silver job (2026-06-20)

The **PySpark silver job**, standalone: a new `spark/` Docker stack runs single-node `local[*]` Spark with the hadoop3 GCS connector (jar `chmod 644`'d so the non-root `spark` user can load it) reading bronze gz via ADC. `silver_events.py` reads with an **explicit schema** (no inferSchema), flattens the envelope (`actor.id`, `repo.name`, …), **dedupes on `event_id`**, casts `created_at`→TIMESTAMP, derives `event_date`/`event_hour`, and writes Hive-partitioned Parquet to a new **silver GCS bucket** (`gs://devpulse-dp2622-silver/events`) with **dynamic partition overwrite** (partition-scoped, idempotent — the lake analog of Day 5's BQ decorator). Hit + fixed an OOM on parallel GCS upload under `local[*]` by baking `spark.driver.memory=4g`. Reconciled 2024-01-01 15:00 against a raw `Counter` (180,387 raw → **180,386** deduped, 1 dupe removed) and proved idempotency (re-run = 12 files, 0 orphaned staging). First **pytest** on the pure `transform_events` (dedupe + malformed-timestamp→NULL) passes in-container. `payload` deliberately dropped from silver for now (varying shape + author-email PII).

## Day 7 — Silver → BigQuery + wire Spark into Airflow (2026-06-22)

**The swap**: new `transform/load_silver.py` loads one hour's silver Parquet → BigQuery `silver_events` (HOUR-partitioned on the in-file `created_at` — `partitionBy` strips `event_date`/`event_hour` into the path; malformed-`created_at` rows quarantine in `__HIVE_DEFAULT_PARTITION__` and aren't loaded) via a **free native Parquet load job** into the `$YYYYMMDDHH` decorator (`WRITE_TRUNCATE`); reconciled to 180,386. Wired the Spark job into the DAG as a **DockerOperator** (`silver_transform`) that launches the `devpulse-spark` image on the host daemon over a mounted Docker socket (DooD: host-root surface, host-path injection via `HOST_PROJECT_DIR`/`HOST_ADC`, `mount_tmp_dir=False`), then `load_silver`; **retired the `Counter`** (deprecated `event_counts.py` — the API still reads its table) and **dropped `max_active_runs=1`** (Spark lifts the memory bound it fenced). Proved full-chain idempotency through `dags test` (re-run = 12 GCS files + 180,386 BQ rows, steady). Added `requirements-dev.txt` + a pure-Python `load_silver` unit test (the path/decorator padding asymmetry).

## Day 8 — Phase 2 / dbt bootstrap (2026-06-26)

Stood dbt up in **its own Docker image** (`dbt/Dockerfile` + compose, keyless oauth/ADC) after installing `dbt-bigquery` into the pipeline `.venv` broke it (downgraded `google-cloud-storage`, broke `black`) — recreated the `.venv` clean; isolation is also the production form (dbt later runs as an Airflow DockerOperator + CI image). Added the `devpulse_gold` BQ dataset to **Terraform** (region-matched to silver + pipeline-SA `dataEditor` grant). Scaffolded the dbt project (`dbt_project.yml` with per-layer materializations `staging:view`/`marts:table`, gitignored `profiles.yml`); declared `silver_events` as a dbt **source** (the layer seam); built `stg_events` (staging **view**, recovers `event_date`/`event_hour` from `created_at`, reconciled 180,386) and `dim_event_type` (first **table** + first `ref()`, 15 types); 7 schema tests (`not_null`/`unique`/`accepted_values`) — **`dbt build` green**, `dbt docs` lineage generated, `sqlfluff` (dbt templater) clean. dbt-in-Airflow gate deferred to Phase 2 Week 5.

## Day 9 — Deepen the star schema: the remaining dimensions (2026-06-29)

Added `dbt_utils` as a dbt **package** (`packages.yml` + committed `package-lock.yml`, pinned 1.4.1; code in gitignored `dbt_packages/`). Built `dim_repo`/`dim_actor` as **tables** off `ref('stg_events')` — md5 **surrogate keys** (`generate_surrogate_key`) beside the natural keys, **Type-1 latest-wins** on the slowly-changing `repo_name`/`actor_login` via `QUALIFY ROW_NUMBER()`; grain reconciled (55,245 repos / 39,030 actors, no fan-out), `dim_actor` holds the PII line (public id+login only, no email). Built `dim_date` as a **generated** `date_spine` calendar (366 days, gap-free, **smart `YYYYMMDD` `date_key`** — the deliberate non-surrogate; 0 bytes scanned). Full `dbt build` green (**PASS=28**), lineage docs regenerated; sqlfluff clean (scoped `noqa` for `date_spine`'s blank lines + the `quarter` identifier) and fixed the Day 8 `accepted_values` dbt-1.11 deprecation (args → `arguments:`). `relationships` tests deferred with the fact. Teardown done (`terraform destroy`, 9 resources).

## Day 10 — The incremental `fact_events` + the first mart (2026-07-06) · [plan](daily/day-10.md)

**The center of the star**: built `fact_events` as the first **incremental** model (`merge` on `unique_key='event_id'`, `partition_by=event_date`, `cluster_by=event_type`, `on_schema_change='fail'`) off `ref('stg_events')` — factless, `event_id` the **degenerate** key, four FKs (`repo_sk`/`actor_sk`/`date_key`/`event_type`) **regenerated deterministically** from the natural keys (no dim join — `generate_surrogate_key` is a pure md5 reproducing the dims' keys). Added the deferred **`relationships`** tests (each FK → dim PK, a compiled left-anti-join) + `unique`/`not_null` on the grain — all 10 fact tests pass first try (`dim_date`'s gap-freeness is what lets its `date_key` test pass). Built the first serving mart **`trending_repos_daily`** (table): `stars = count(WatchEvent)` per `(repo, day)`, joined to `dim_repo`, `rank()` leaderboard, grain-guarded by `unique_combination_of_columns`. `dbt build` green (**PASS=48**). **Idempotency proven the incremental way**: a 2nd `dbt run -s fact_events` flipped `CREATE TABLE`→`MERGE`, row count steady at **180,386** (0 inserted — merge on `unique_key` can't duplicate). Banked the honest finding that the MERGE scanned *more* than the full build (8.5→40.4 MiB) — over one partition with a full-hour lookback there's nothing to prune; merge's win is a scale phenomenon. Lint clean, **no new `noqa`**. Teardown done.

## Day 11 — Complete the gold layer: the last two marts + enrichment seed (2026-07-07) · [plan](daily/day-11.md)

**Completed the gold serving layer** (Phase 2 modeling milestone): the last two marts + the enrichment **seed** that unblocks one. `language_momentum` needed a language the events don't carry (payload dropped Day 6, shape+PII) → brought it in as a committed dbt **seed** `repo_languages` (`repo_id→language`, INT64-pinned via `column_types`; seeds = the 5th dbt object type) — an honest **stand-in** for a GitHub-repos-API `repo_metadata` source (mart reads it via `ref()`, so the swap never touches SQL). Built `language_momentum` (table): `fact_events`→**`dim_repo`** (recover the natural `repo_id` the lean fact drops)→**LEFT JOIN** seed→`COALESCE('Unknown')` (never INNER — a partial seed + INNER silently drops most events), `event_count`/`active_repos` per `(language,day)` + `lag()` momentum + `rank()`; reconciled `SUM(event_count)`=**180,386** (no drops; 4 seeded repos matched, 178,081 `Unknown`, `active_repos` sum 55,245 = `dim_repo` grain). Built `contributor_leaderboard` (table): `fact_events`→`dim_actor`, filtered to a **contribution event-type allowlist** (excl. Watch/Fork), flat `count(*)`+`rank()`; reconciled `SUM(contributions)`=**163,953** = fact filtered to the allowlist (16,433 non-contributions excluded). Full `dbt build` green (**PASS=69**: 9 models + 59 tests + 1 seed), docs regenerated (all 3 marts fan off the fact, seed feeds `language_momentum`). Banked finding: the single-hour firehose is dominated by **bot/automation repos** with opaque names — a hand-authored language seed can't be accurately populated by inspection, the concrete argument for real API enrichment at scale. Corrected the day-11 plan's Step 2 (fact carries no `repo_id` → route via `dim_repo`). Lint clean (scoped `noqa: RF04` for `language`; LT02 fix). Teardown done.

## Day 12 — dbt build as an Airflow DAG gate (2026-07-08) · [plan](daily/day-12.md)

**The quality gate** (first day fully under coach mode — Bryan wrote the operator): a `dbt_build` **DockerOperator** appended to `devpulse_ingest` (`... >> load_silver >> dbt_build`), running `command="dbt build"` on the existing `devpulse-dbt` image — the compose contract hand-translated (no entrypoint/`env_file` carry; `DBT_ENV` mirrors every `env_var()` the profile + `_sources.yml` render; project dir = the DAG's only **read-write** bind, ADC `:ro`). **Green path:** pinned backfill (`-s = -e = 2024-01-01T15:00`) → all 5 tasks green, `ingest` skipped ("Already in bronze"), task log **PASS=69** — tests now run *in-pipeline*, and the orchestrated re-run doubled as the full-chain idempotency proof (every count steady). **Red path, delivered by reality:** an earlier unpinned trigger processed the *current* interval (hour **19** — we misread the interval end as its start; `/verify-pipeline` had documented the semantics since Day 5), landing a stray 164,650-row 2026 hour that the static 2024 `dim_date` spine orphaned → 4 `relationships` tests red, run failed, retries re-failed identically (retries ≠ data fixes). Cleanup walked all four layers: GCS objects, `bq rm` the *real* partition (first attempt hit a nonexistent one — **silently**; `INFORMATION_SCHEMA.PARTITIONS` caught it), then `--full-refresh` because **a merge can't un-merge**; restored to PASS=69/180,386 and re-verified end-to-end (`SUM`s 180,386 / 163,953 / 7,236=WatchEvents; dims 55,245/39,030/366/15). Scripted sabotage consciously skipped — the organic incident was the stronger red-path proof (5 decisions banked). Also: fixed pre-existing lint drift in 7 untouched files (`ruff --fix` + `black`; import sorting). Coach-mode ledger: Bryan implemented the operator through 3 review rounds (2 real catches self-fixed: `$`-quoting, host-path); one level-3/4 handout (`command="dbt build"`) logged in `warmups.md`. Warm-up drill #1 (`bronze_key`/`ingest_hour`) graded 🔶. Teardown done.

## Day 13 — Great Expectations bronze→silver gate: the counted quarantine (2026-07-09) · [plan](daily/day-13.md)

**The quality layer dbt can't reach** (coach mode — Bryan wrote the validator through ~10 review rounds): new `quality/` stack (5th per-tool image; GE **1.18.2**/pandas 2.3.3/gcsfs pinned; file-backed GX context, suite JSON committed as the reviewable contract — needed a `*.json` unignore in the root `.gitignore`). `validate_silver.py`: an **8-expectation suite** over the hour's silver Parquet (columns-match-set, strict `not_null` on the contract trio honoring the Day 6 SCHEMA IOU, `mostly=0.9999` null-rate floors on actor/repo, `unique` event_id, 50k–900k volume fence) **plus the counted checks the suite can't express** — quarantine count (global, read with `partitioning=None`) expected 0, bronze raw stream-counted (lines only, payload never parsed — PII), and the conservation identity **180,387 = 180,386 + 0 + 1** (residual = dedupe, thresholded, negative fails hard); pure logic split into `checks.py` with 6 host pytests; exit code = composite verdict, single source of truth with the log. Wired as the **third DockerOperator** between `silver_transform` and `load_silver` (RW mount — GE writes a Validation Result every run) → **6-task chain green**, counts in the task log, PASS=69 downstream. **Red path (scripted fixture)**: one malformed row rode bronze→quarantine→**gate red, run failed, `load_silver`/`dbt_build` `upstream_failed`, BQ untouched** — and the drill caught **three real validator bugs** (quarantine unreadable by hive discovery — `ArrowInvalid`, and the *narrow* `except` crashed loud where a broad one would have passed silent; log said PASSED while the exit code said fail; `raw` undercounted vs Spark's glob), all fixed and committed. **Lifecycle lesson observed live:** deleting the cause didn't clear the quarantine (dynamic overwrite can't touch a partition absent from new data) — manual prefix delete; then the run's **pending retry self-healed the chain to green** (retries finish what humans fix). Session also opened on 2 stray 2026 runs (paused flag lives in the Airflow DB, Day 12 left it unpaused) — killed pre-load, two-command cleanup, the placement argument by accident. Verified end-to-end: silver/fact **180,386**, marts 180,386 / **163,953** / 7,236 = WatchEvents, grains 55,245, 12 files / 0 staging, **PASS=69**. Alerting + run-metadata → Day 14. Warm-up #2 (`load_silver`) graded 🔁. Teardown done.
