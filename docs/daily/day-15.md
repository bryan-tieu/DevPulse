# Day 15 — Serve the star: FastAPI over the gold marts, parameterized, cost-capped, paginated, cached

> **Phase 3 · Week 6 (🏁 the guaranteed finish line begins).** Picks up from Day 14 (Phase 2 closed: the 7-task chain gates on GE + dbt, pages the same minute a data failure lands, and remembers every run in `pipeline_run_metadata`). The warehouse is now trustworthy and observable — but the only thing that has ever *read* the gold marts is a `bq` console. Today the pipeline grows its product surface: a real FastAPI service over the three marts, replacing the Phase-0 thin slice (`/event-counts` — an f-string query against a deprecated silver table) with the serving layer the blueprint promised. This is also the day two 🔐 backlog items land: **parameterized queries** and **`maximum_bytes_billed` on every query job**.
>
> **Scope call (read first):** IN — a reworked `api/` package: shared BQ client via FastAPI dependency injection, a query layer where **every value is a query parameter** and **every job carries a bytes cap**, three mart endpoints (`/trending`, `/languages/momentum`, `/leaderboard`) with validated pagination and Pydantic response models, a hand-rolled TTL cache, a `/runs` ops endpoint reading `pipeline_run_metadata` via **free `list_rows`**, and retirement of the Phase-0 slice (`transform/event_counts.py` + `run.py` + the old endpoint). OUT — the dashboard (Day 16 — it consumes today's API), GitHub Actions CI (later this phase), auth + rate limiting + CORS + dependency pinning (🔐 pre-deploy items; today the API binds to localhost and is never exposed), `/repos/{id}` detail endpoint (add when the dashboard needs it), Cloud Run deploy (Phase-3 stretch).
>
> **The day's central decision, stated up front and honestly:** *should a user-facing API query the warehouse directly?* Today: yes — the API queries BigQuery per request, because traffic is one developer, marts are KB-to-MB scale, and freshness-after-each-run matters more than milliseconds. **Rejected (and when each wins):** replicating marts into a serving DB (Postgres/Cloud SQL) — wins the moment real users arrive, because warehouses bill per query and answer in seconds while serving DBs answer in milliseconds at fixed cost; BI Engine / warehouse-native acceleration — wins for BI-tool dashboards that must stay inside BQ; pre-exported static JSON behind a CDN — wins when a small fixed query set meets big anonymous read traffic. The production-vs-simplified line, said loudly: **direct-BQ serving is the simplification** — the honest at-scale shape is "the warehouse computes, a serving store serves," and today's clean query-layer boundary (`api/queries.py`) is exactly the seam where a serving DB would slot in. Within that: values are **query parameters, never string-interpolated** (the injection line); identifiers (table names) *can't* be parameterized — they come from config constants only, never from request input, and that distinction is the interview answer; and the TTL cache is an in-process dict — honest for one worker, named as Redis / run-id-keyed invalidation at scale.

## Environment notes (Windows 10 · PowerShell · Docker Desktop)
Carries from Day 14 — **no new image**; today lives on the **host `.venv`** (FastAPI + `uvicorn` are already in `requirements.txt` from the Day-3 slice). Docker is needed only for the rehydrate chain (Spark + dbt); **Airflow stays down all day** — the API reads marts, it doesn't orchestrate.
- **Keyless ADC unchanged** — the API authenticates as you, exactly like `load_silver.py`. 🔐 No new secrets; the API is localhost-only and unauthenticated **today**, acceptable only because it is never exposed — auth + rate limiting + CORS + pinned deps are the pre-deploy gate (decisions.md security backlog), restated in step 7's docs.
- **The daily `terraform destroy` emptied everything** → `/start-session` chain first, through **PASS=69** — the API has nothing to serve until the marts exist. New env var: **`BQ_GOLD_DATASET=devpulse_gold`** into the gitignored `.env` + a `config.py` constant (the existing `BQ_DATASET` is the silver dataset; the API must not guess dataset names inline — config constants are what makes "identifiers never come from users" checkable).
- **Run the API from the repo root**: `python -m uvicorn api.main:app --reload` (`config.py` imports resolve from root). Test with the auto-docs UI at `http://127.0.0.1:8000/docs` — on Windows it beats `curl.exe`+JSON squinting, and it's the day's free deliverable: response models make the docs correct, not just present.
- **Orientation — the two FastAPI ideas today that the Day-3 slice never used:** *dependency injection* (`Depends`) gives every endpoint a shared BQ client the tests can swap for a fake — that's what makes endpoint tests run without GCP; *response models* (Pydantic) declare the output schema so FastAPI validates it, documents it, and strips anything undeclared — the API-layer twin of "explicit schemas, no autodetect."

## Focus
Rebuild `api/` as a real service: (1) `api/queries.py` — pure builder functions returning `(sql, params)` + one executor that stamps **`maximum_bytes_billed`** on every `QueryJobConfig`; (2) three mart endpoints with validated `limit`/`offset` pagination and Pydantic response models — `/trending` (stars + daily_rank from `trending_repos_daily`), `/languages/momentum` (event counts + momentum_delta from `language_momentum`), `/leaderboard` (contributions from `contributor_leaderboard`); (3) a hand-rolled TTL cache in front of the executor with an `X-Cache: hit|miss` header; (4) `/runs` — last-N pipeline runs from `pipeline_run_metadata` via free `list_rows` (Day 14's artifact gains the API consumer it was promised); (5) the Phase-0 event-counts slice deleted. End state: `http://127.0.0.1:8000/docs` shows four documented endpoints; every value reaching SQL is a bound parameter; every query job is cost-capped; repeated requests hit cache instead of BQ; endpoint sums reconcile to the marts (**7,236 / 180,386 / 163,953**).

## Build steps
Execute in order; one small commit per step. Steps 0 and 6 are setup/verify, not commits. Coach-mode markers per CLAUDE.md: 🧑‍💻 = Bryan implements (Claude frames, reviews, hints on request); 🤖 = Claude may scaffold.

0. **Rehydrate + baseline green.** 🤖 `/start-session` chain through **PASS=69** (terraform apply → ingest canonical hour → Spark transform → `load_silver` 180,386 → dbt deps + build). Airflow stays down. Add `BQ_GOLD_DATASET=devpulse_gold` to `.env`; host `.venv` active; `python -m uvicorn --version` sanity check. *(Setup, not a commit.)*

1. **The query layer — parameters and the cap.** 🧑‍💻 New `api/queries.py`: for each mart a **pure builder** `build_<mart>_query(date_key, limit, offset) -> tuple[str, list[bigquery.ScalarQueryParameter]]` — SQL uses `@named` placeholders for **every value**; table names composed from `GCP_PROJECT`/`BQ_GOLD_DATASET` config constants with a comment stating the rule (*identifiers can't be parameterized in any SQL engine — so identifiers must never come from request input; values always bind*). One executor `run_query(client, sql, params) -> list[dict]` building `QueryJobConfig(query_parameters=..., maximum_bytes_billed=...)` — cap at **100 MB** (marts scan KBs; the cap exists to fail loudly if a query ever grows teeth, and it closes a Day-7-era backlog item). The pure/executor split is the pytest seam: builders are asserted on SQL text + param names/types with zero GCP. Gotcha to expect: `ScalarQueryParameter` wants an explicit BQ type per param (`"DATE"`, `"INT64"`) — autodetect-by-Python-type exists and is the same trap as `inferSchema`; be explicit. **Verify:** builder unit tests green on host. *Commit: `add parameterized query layer with cost cap`.*

2. **`/trending` — the first real endpoint.** 🧑‍💻 Rewrite `api/main.py`: a `get_bq_client()` dependency (module-level client, injected via `Depends` so tests can override); `GET /trending` with `date: date = 2024-01-01` (the canonical hour's date as default), `limit: int = Query(20, ge=1, le=100)`, `offset: int = Query(0, ge=0)`; a Pydantic `TrendingRepo` response model mirroring the mart's columns (`repo_id`, `repo_name`, `stars`, `daily_rank`); `ORDER BY daily_rank` — **an API without a deterministic ORDER BY has broken pagination by construction** (pages can overlap/skip between requests; say it in a comment). The teaching, stated honestly: FastAPI's validation bounds (`ge`/`le`) are the API edge rejecting garbage with a 422 *before* it reaches the warehouse — the same fail-loudly line, moved to the front door; and **`LIMIT`/`OFFSET` does not reduce BigQuery bytes scanned** (columnar engines scan the column regardless — pagination shapes the payload, not the bill; keyset pagination is the deep-page fix in a serving DB, named not built). Gotcha: BQ returns `date_key` as a Python `date` — Pydantic handles it, but only if the model says `date`, not `str`. **Verify:** `uvicorn` up → `/docs` renders the model; top row matches `bq` console for the mart; a `limit=101` request 422s; pytest via `TestClient` + `dependency_overrides` with a fake client (no GCP in tests). *Commit: `add trending endpoint with validated pagination`.*

3. **The other two marts — reps.** 🧑‍💻 `/languages/momentum` (`language`, `event_count`, `active_repos`, `momentum_delta`, `daily_rank` — `momentum_delta` is `Optional`: it is NULL over one ingested hour, the mart's degenerate-window honesty surfacing in the API schema) and `/leaderboard` (`actor_id`, `actor_login`, `contributions`, `daily_rank`). Same shape as step 2 — this is deliberately a rep, not a new lesson; expect it to go 3× faster, and notice what that says about the pattern. 🔏 The leaderboard serves `actor_login` — public username, the Day-9 PII line; nothing from `payload` exists anywhere downstream to leak. **Verify:** both endpoints in `/docs`; endpoint-vs-warehouse reconciliation — sum each measure across all pages (script or generous `limit`) and match the marts: stars **7,236**, event_count **180,386** (the `Unknown` language bucket included — an INNER-joined API would be quietly wrong here, same rule as the mart itself), contributions **163,953**. *Commit: `add language momentum and leaderboard endpoints`.*

4. **The cache — freshness as policy.** 🧑‍💻 Hand-rolled TTL cache (~20 lines, no new dependency): dict keyed on the query signature (`(sql, tuple of param values)`), storing `(expires_at, rows)` off `time.monotonic()` (wall clocks jump; monotonic doesn't — say why in a comment); wrap the executor; **TTL 300 s**; add `X-Cache: hit|miss` response header so cache behavior is observable from outside (the Day-14 lesson — artifacts for machines — applied to HTTP). The teaching: a TTL is a **freshness policy, not an optimization** — marts change only when the pipeline runs, so the honest invalidation key is the latest `run_id` in `pipeline_run_metadata` (named, not built); TTL is the accepted approximation. Flag loudly: in-process cache = per-worker — two uvicorn workers means two caches; the production shape is shared (Redis) or HTTP caching headers in front. Keep the cache class **pure** (injectable clock) and pytest expiry with a fake clock. **Verify:** two identical requests → `miss` then `hit`, second visibly faster and (check the BQ job history) no second query job; different params → `miss`. *Commit: `add ttl cache over mart queries`.*

5. **`/runs` — the ops window, on the free path.** 🧑‍💻 `GET /runs?limit=10`: read `devpulse_silver.pipeline_run_metadata` via **`client.list_rows`** — a free tabledata read, deliberately **not** a query job (the Day-14 cost decision — free load jobs in, free list reads out — completing its arc; the table is tiny and unpartitioned, so "read all, sort in Python by `recorded_at` desc, slice" is correct, and a comment must say at what scale that stops being true: when the table outgrows a single cheap read, it earns partitioning and a real query). Response model with the row's fields (run_id, logical_date, per-task states, counts, composite verdict, recorded_at) — the API now serves the pipeline's memory: the green/red/green history from Day 14's proof becomes visible JSON. **Verify:** the Day-14 rows appear, newest first; BQ job history shows **no query job** for this endpoint. *Commit: `add pipeline runs endpoint over run metadata`.*

6. **Retire the Phase-0 slice.** 🤖 (repo chore — Bryan reads the diff): delete `transform/event_counts.py` and `run.py` (the old `/event-counts` endpoint died in step 2's rewrite); sweep docs references. The `hourly_event_counts` table stopped being recreated after Day 4 and lives in a dataset `terraform destroy` empties nightly — nothing to drop. This closes the decisions.md line "lives until the Phase 3 API rework." **Verify:** `grep` finds no live references (docs/history mentions stay — history is history); pytest + `ruff` still green. *Commit: `retire phase-0 event counts slice`.*

7. **Reconcile + lint.** `/verify-pipeline`: silver **180,386**, marts SUMs **180,386 / 163,953 / 7,236**, **PASS=69** (dbt untouched today — this proves it); host pytest green (17 existing + today's builder/endpoint/cache tests); `ruff` + `black` clean; no `.env` values or artifacts tracked. *(Setup/verify, not a commit.)*

8. **Docs + status + teardown.** 🤖 `decisions.md`: direct-BQ serving steelman (when the serving DB wins), values-vs-identifiers parameterization line, `maximum_bytes_billed` (backlog item closed — update the security-backlog section), LIMIT-doesn't-cut-scan, TTL-as-freshness-policy + per-worker honesty, free `list_rows` for the ops read. `tradeoffs.md` gains the FastAPI/direct-serving entry. `skills-map.md`: API serving layer row. `history.md` + CLAUDE.md status (next up = **Day 16: dashboard over today's API**; reference values gain the API endpoints + cap). Teardown: Spark/dbt containers down, **`terraform -chdir=terraform destroy`**. *Commit: `document api serving decisions and update status`.*

## Constraints & conventions
- 🧱 **One lesson: serving a warehouse without getting burned** — parameterize the values, cap the bytes, validate at the edge, paginate deterministically, cache deliberately. Dashboard, CI, auth, deploy → named, deferred.
- 🚫 **No f-string SQL values anywhere** — the step-1 builders are the only place SQL is assembled, and `grep -n "f\"\"\"" api/` finding a value interpolation is a step-1 failure. **No `SELECT *`** (bytes billed = columns scanned — name the columns). **No new serving infra** (no Postgres, no Redis, no BI Engine — today's scale doesn't earn them; articulating *when they'd win* is the deliverable). **No `insert`/write paths in the API** — it is read-only over gold + run metadata.
- 🔗 **Tests are the dependency-injection dividend:** builders tested pure (SQL + params, no GCP); endpoints tested via `TestClient` + `dependency_overrides` with a fake BQ client; cache tested with a fake clock. An API test that needs live GCP is a design smell today.
- 🔏 **PII line:** the API serves `repo_name`, `actor_login` (public), counts, and pipeline metadata — nothing from `payload` exists downstream to serve. `/runs` exposes op counts and task states only.
- 💸 **Cost shape:** every mart query is a billed query job (10 MB minimum applies — pennies, but the cap + cache are the habit); `/runs` is a free `list_rows`; `maximum_bytes_billed=100 MB` on every job; no `SELECT *`; teardown, always.
- 🔐 **Backlog moved and carried:** parameterized queries + `maximum_bytes_billed` **land today**; auth + rate limiting + deliberate CORS + pinned deps remain the pre-deploy gate (API stays localhost-only until then); pipeline-SA impersonation + silver-bucket grant still pending.
- 📐 **Lint:** ruff + black; type hints on every signature; pure/IO split enforced (builders, cache, row-sorting pure; client calls thin); comments carry *why* (identifiers-vs-values, ORDER BY for pagination, monotonic clock, list_rows-not-query, the 100 MB cap).
- 📦 **Six commits, imperative mood:** `add parameterized query layer with cost cap` · `add trending endpoint with validated pagination` · `add language momentum and leaderboard endpoints` · `add ttl cache over mart queries` · `add pipeline runs endpoint over run metadata` · `retire phase-0 event counts slice` · plus the docs commit.

## Done criteria

> **Session 1 (2026-07-12 → 13, past midnight) closed after step 1** — commit `d926a22` (query layer + `config.py` gold constant + 10 pytests, suite 27 green, lint clean). Plan corrections found in-session: **`date_key` is the `YYYYMMDD` INTEGER smart key, not DATE** (step 2's `date: date` default needs an edge conversion decision — the plan's claim lost to `bq show --schema`); step-1 verification gained a **server-side dry run** (free; validated the three queries at 331,541 / 1,444,080 / 167 bytes). Findings banked in decisions.md: the DI shadow-binding bug (`run_query` rebuilt its injected client — caught red-first by a mock test), values-vs-identifiers, cap-as-tripwire. One L4 handout (the mock-client test) logged in warmups.md — same past-midnight window as Day 14's. Steps 2–8 next session.
>
> **Session 2 (2026-07-13) closed after step 2** — commit `d22e8c4` (`/trending` + the `api/main.py` rewrite; old f-string `/event-counts` dead). Date-edge decision resolved: **ISO `?date=` typed `date`, converted to the int smart key at the edge** (impossible dates 422 instead of 200-empty; why in decisions.md). Error handling verified against the artifact: 1-byte-cap experiment → `bytesBilledLimitExceeded` reason surfaces as fallback `InternalServerError`; handler discriminates on `reason`, not class. `get_bq_client()` = `lru_cache` singleton behind `Depends`. Live-verified against real BQ (rank-1 row + both 422 edges) + 7 endpoint tests via `dependency_overrides` (suite **34**, ruff/black clean) — **tests were an L4 handout ("Write the test."), 3rd test-file handout in 4 days; logged in warmups.md, "write tests cold" drill now due.** ⚠️ Tests + `requirements-dev.txt` (`httpx2`) still uncommitted — first commit tomorrow. ⚠️ Divergence to confirm deliberate: `?date=` is **required** (plan defaulted it to 2024-01-01). Steps 3–8 remain; step 3 framed (momentum_delta Optional via `bq show --schema` first; Bryan writes the tests; reconciliation 7,236 / 180,386 / 163,953).

- [x] `api/queries.py`: pure builders + executor; **every value a bound `@param`**, every `QueryJobConfig` carries `maximum_bytes_billed`; builder pytest green with zero GCP. *(10 tests incl. the mock-client DI test; dry-run validated all three queries server-side.)*
- [ ] Four endpoints live and documented at `/docs` with Pydantic response models: `/trending`, `/languages/momentum`, `/leaderboard`, `/runs`; `limit=101` → **422**; all pagination under a deterministic ORDER BY.
- [ ] Endpoint-vs-warehouse reconciliation: paged sums = mart truths — stars **7,236** · event_count **180,386** (incl. `Unknown`) · contributions **163,953**.
- [ ] Cache proven: identical request → `X-Cache: miss` then `hit`, **no second BQ query job** in job history; expiry pytest green with fake clock.
- [ ] `/runs` returns Day-14's green/red/green rows newest-first via `list_rows` — **zero query jobs** for the endpoint in BQ job history.
- [ ] Phase-0 slice gone: `transform/event_counts.py` + `run.py` deleted, no live references; old `/event-counts` endpoint no longer exists.
- [ ] `/verify-pipeline` holds untouched: silver **180,386**; marts **180,386 / 163,953 / 7,236**; **PASS=69**.
- [ ] `ruff` + `black` + host pytest (17 + today's) clean; `.env` additions untracked.
- [ ] `decisions.md` (≥5 entries incl. security-backlog update), `tradeoffs.md`, `skills-map.md`, `history.md`, CLAUDE.md status updated → next up **Day 16: dashboard**.
- [ ] Teardown: containers down, `terraform destroy` clean (9 destroyed).

## Learning goals
1. **Parameterized queries, and the values-vs-identifiers line** — why binding values kills injection structurally (the value never enters the SQL text), why identifiers *can't* be bound in any engine, and therefore why table/column names must only ever come from config — the complete interview answer, not just "use parameters."
2. **`maximum_bytes_billed` as a per-query circuit breaker** — cost control at the job level fails an over-budget query *before* it scans; contrast with billing alerts (after the fact) and IAM caps (too blunt); the habit matters at 10 MB scale because it's unchangeable muscle memory at 10 TB scale.
3. **Serving straight from the warehouse vs a serving database** — per-query billing, seconds-latency, and concurrency limits make direct-BQ serving a small-scale simplification; being able to name the trigger (real users) and the migration seam (the query layer built today) is the production-thinking answer.
4. **Pagination in a columnar warehouse** — LIMIT/OFFSET shapes the response, not the bytes scanned; deterministic ORDER BY is what makes pages coherent at all; keyset pagination fixes deep-offset cost in serving DBs.
5. **Caching as a freshness policy** — the TTL encodes "how stale is acceptable," and the honest invalidation key for batch-updated marts is the pipeline's own run metadata; in-process vs shared cache is a deployment-topology decision, not an implementation detail.
6. **Dependency injection as the testability seam** — swapping the BQ client via `dependency_overrides` is the API-layer version of the pure/IO split the pipeline has enforced since Day 1.
7. **Free vs billed read paths** — `list_rows` tabledata reads vs query jobs, completing Day 14's free-load-jobs arc: cost-aware engineers know *which* API they're calling, not just what data comes back.
